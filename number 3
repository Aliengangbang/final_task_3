# Вычисление расстояния Хэмминга между введённой строкой и набором строк в файле
# сортировка результата по возрастанию и запись в файл
str1= input('Введите строку: ') # Вводим строку str1
res_list=[[0,'-']] # Создадим список для хранения результатов 
del res_list[0] # Очистим список
with open('input_file','r') as f:
    for i, line in enumerate(f,1):   # Читаем данные из файла
        instr = line.rstrip('\n')
        hamming = 0
        if len(str1) == len(instr):   # Если длина строк одинаковая, то вычисляем расстояние Хэмминга
            for j in range(len(str1)):  # В цикле посимвольно сравниваем строки
                if str1[j] != instr[j]: # и считаем количество несовпадений
                    hamming += 1
            res_list.append([hamming,instr]) # Добавляем в список расстояние Хэмминга и сравниваемую строку
        else:
            instr=instr+' - Не совпадают длины строк!'  # Если длина строк разная, то запишем сообщение об ощибке
            res_list.append([-1,instr])
res_list.sort() # Сортируем список по расстоянию Хэмминга
with open('output_file','w') as f:  # Записываем результаты в файл
    s='Введена строка: '+str1+'\n'
    f.write(s)
    for elem in res_list:
        s=str(elem[0])+ " " + elem[1]+'\n'
        f.write(s)
print('Работа программы завершена')

